import { ExpressionDirective } from './expression.directive';

describe('ExpressionDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpressionDirective();
    expect(directive).toBeTruthy();
  });
});
